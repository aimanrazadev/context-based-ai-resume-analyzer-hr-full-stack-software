export default function ConfirmDialog({ open, title = "Confirm action", message, onConfirm, onCancel }) { if (!open) return null; return <div role="dialog" aria-modal="true" aria-label={title} style={{ position: "fixed", inset: 0, background: "#0f172a66", display: "grid", placeItems: "center", zIndex: 1000 }}><div style={{ background: "white", borderRadius: 16, padding: 20, width: "min(420px,90vw)" }}><h2>{title}</h2><p>{message}</p><div style={{ display: "flex", justifyContent: "end", gap: 10 }}><button type="button" onClick={onCancel}>Cancel</button><button type="button" onClick={onConfirm}>Confirm</button></div></div></div>; }
